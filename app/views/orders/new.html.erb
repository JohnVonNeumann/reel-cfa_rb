<!-- make suse of the _form partial we created earlier -->
<div class="depot_form">
  <fieldset>
    <legend>Please enter your details: </legend>
    <%= render 'form', order: @order %>
  </fieldset>
</div>
